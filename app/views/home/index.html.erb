<h1>Birthdays 🎉</h1>
<div class="center row">
  <div class="small-9 large-6 small-centered large-centered columns">
    <div class="panel radius">
      <div class="row">
        <h4 id="index-text">Are you forgetting how old your friends and loved ones are? Never forget again with Birthdays! Simply add their name and birth date and we'll tell you their age!</h4><br>
      </div>
      <div class="row">
        <% if user_signed_in? %>
          <div class="small-4 large-4 small-centered large-centered columns">
            <%= button_to "Sign Out", destroy_user_session_path, method: :delete, :class => "button info large expand radius" %>
          </div>
        <% else %>
          <div class="small-4 large-4 small-centered large-centered columns">
            <%= button_to "Log In", new_user_session_path, method: :get, :class => "button info large expand radius" %>
            <%= button_to "Register", new_user_registration_path, method: :get, :class => "button info large expand radius" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
